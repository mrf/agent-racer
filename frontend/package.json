{
  "name": "agent-racer-frontend",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage"
  },
  "devDependencies": {
    "@playwright/test": "^1.58.2",
    "@vitest/coverage-v8": "^3.0.0",
    "jsdom": "^25.0.1",
    "vite": "^7.3.1",
    "vitest": "^3.0.0"
  }
}
